// Throwaway probe for VLCN-999 AC-3. Deleted in the next commit.
import { describe, expect, it } from "vitest";

describe("VLCN-999 AC-3 probe", () => {
  it("fails on purpose, to prove the test leg can turn the gate red", () => {
    expect(1).toBe(2);
  });
});
